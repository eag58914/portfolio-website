import React from 'react';
import style from './Title.module.css';

const Title = () => {
	return (
		<div className="flex-container">
			<h1 className={style.titleText}>EGA</h1>
		</div>
	);
};

export default Title;
